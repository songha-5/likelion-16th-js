// --------------------------------------------------------------------------
// 실습: JavaScript를 사용한 인증 방법
// --------------------------------------------------------------------------


// --------------------------------------------------------------------------
// 핵심 요약
// --------------------------------------------------------------------------
// - 기본 인증은 'Basic' 키워드와 Base64 인코딩된 아이디:비밀번호를 사용합니다.
// - btoa()는 바이너리를 ASCII(Base64)로, atob()는 그 반대로 변환합니다.
// - OAuth 인증은 'Bearer' 키워드와 함께 서버에서 발급받은 엑세스 토큰을 사용합니다.
// - 인증(Authentication)은 신원 확인, 권한(Authorization)은 접근 범위 제어를 의미합니다.
// - 보안을 위해 브라우저 환경(Client-side)에 토큰이나 비밀번호를 직접 노출하지 않도록 주의합니다.
// --------------------------------------------------------------------------