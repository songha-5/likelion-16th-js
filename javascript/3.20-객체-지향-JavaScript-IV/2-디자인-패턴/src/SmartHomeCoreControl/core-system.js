// --------------------------------------------------------------------------
// ⚙️ 코어 시스템 : 디자인 패턴들이 모여 있는 비즈니스 로직 모듈
// --------------------------------------------------------------------------

/**
 * [Singleton 패턴] - 중앙 제어 허브
 * 시스템 전반에서 단 하나의 객체만 공유하도록 보장하는 '유일 관리자'입니다.
 * 
 * 핵심 이점
 * 1. 자원 통합: 설정이나 상태를 여러 곳에서 중복 생성하지 않고 한 곳에서 관리합니다.
 * 2. 접근 제어: 클로저를 통해 내부 데이터를 보호하며 정해진 통로(get)로만 접근을 허용합니다.
 * 3. 효율성: 인스턴스가 실제 필요한 시점에 생성되어 메모리를 최적화합니다.
 */
export const CentralHub = (() => {
  let instance = null
  const singleton = { id: 'HUB-PREMIUM-V4', version: '1.0.2' }

  return {
    get: () => {
      // TODO 1: 여러 번 요청해도 singleton만 반환하도록 설정
      
    }
  }
})()

/**
 * [Observer 패턴] - 시스템 전파망
 * 상태 변화를 감시하고 이벤트 발생 시 구독자들에게 소식을 알리는 '방송국'입니다.
 * 
 * 핵심 이점
 * 1. 결합도 해제: 알림을 보내는 쪽과 받는 쪽이 서로의 내부 로직을 몰라도 소통이 가능합니다.
 * 2. 중복 방지: Set을 활용해 동일한 구독자가 중복 등록되어 발생하는 불필요한 실행을 차단합니다.
 * 3. 유연한 확장: 새로운 기능을 추가할 때 기존 코드를 수정하지 않고 구독만 하면 됩니다.
 */
export const createLogger = () => {
  // TODO 2-1: 구독자 명단 subscribers 생성 
  

  return {
    // TODO 2-2: 구독자 명단에 추가할 subscribe 메서드 작성
    
    // TODO 2-3: 등록된 모든 구독자에게 동시 방송할 log 메서드 작성

  }
}

/**
 * [Factory 패턴] - IoT 장치 생성 공장
 * 객체 생성의 복잡한 "조리 과정"을 숨기고 타입만 주문받아 완성품을 내놓습니다.
 * 핵심 이점:
 * 1. 코드 단순화: 사용하는 쪽에서 객체 구성 방식(ID 생성, 속성 결합)을 알 필요가 없습니다.
 * 2. 확장성: 새로운 기기가 추가되어도 기존 로직을 건드리지 않고 models 명단만 늘리면 됩니다.
 * 3. 일관성: 모든 객체가 동일한 규격(base)을 갖추도록 강제하여 시스템 에러를 방지합니다.
 */
export const DeviceFactory = {
  /**
   * 요청한 타입에 맞는 장치 객체를 동적으로 생성
   * @param {'light' | 'aircon'} type - 생성할 장치의 종류
   * @returns {Object | null} 생성된 장치 객체 또는 null
   */
  create: (type) => {
    // 공통 뼈대 (고유 식별자 및 타입 정의)
    const base = { 
      id: crypto.randomUUID(),
      type
    }
    
    // 장치별 특화 데이터 (모델 카탈로그)
    const models = {
      light: { name: '지능형 광원 시스템', emoji: '💡' },
      aircon: { name: '대기 정화 에어컨', emoji: '❄️' }
    }

    const modelType = models[type]

    // TODO 3-1: 주문받은 타입이 없다면 null 반환
    
    // TODO 3-2: 주문받은 타입이 있다면 모델 타입에 따라 뼈대와 데이터를 합쳐 완제품 출고

  }
}

/**
 * [Strategy 패턴] - 모드별 실행 전략 꾸러미
 * 상황에 따라 갈아 끼울 수 있는 "로직의 부품화"를 구현합니다.
 * 
 * 핵심 이점
 * 1. 조건문 제거: 방대한 if/else나 switch 문 없이 데이터(키값) 선택만으로 로직을 실행합니다.
 * 2. 독립성: 각 전략(eco, power)이 분리되어 있어 특정 로직을 수정해도 다른 전략에 영향을 주지 않습니다.
 * 3. 교체 편의성: 런타임에 사용자 선택에 따라 알고리즘을 즉시 전환할 수 있습니다.
 */
export const modeStrategies = {
  // TODO 4-1: 에코(eco) 모드인 경우, name을 전달 받아 다음 메시지 출력 (함수)
  //           `${name}이(가) 저전력 에코 알고리즘으로 가동됩니다.`
  
  // TODO 4-2: 파워(power) 모드인 경우, name을 전달 받아 다음 메시지 출력 (함수)
  //           `${name}이(가) 최대 출력 파워 모드로 가동됩니다.`
  
}

/**
 * [Adapter 패턴] - 통신 프로토콜 변환기
 * 호환되지 않는 두 인터페이스 사이를 연결하는 "통역사" 역할을 수행합니다.
 * 
 * 핵심 이점
 * 1. 호환성 해결: 규격이 다른 외부 데이터(Raw)를 내부 시스템이 이해하는 표준으로 변환합니다.
 * 2. 코드 보호: 외부 인터페이스가 변경되어도 어댑터만 수정하면 내부 로직은 보호받을 수 있습니다.
 * 3. 재사용성: 기존 코드를 수정하기 어려운 레거시 시스템을 최신 시스템과 연결할 때 유용합니다.
 */
export const ProtocolAdapter = (raw) => {
  // TODO 5: raw 데이터를 확인해 어댑터 설정
  // raw가 신형(modern)인 경우, 'Matter Standard v1' 출력(변환된 규격)
  // raw가 구형(legacy)인 경우, 'Legacy Bridge' 출력(변환된 규격)
  
}

/**
 * [Decorator 패턴] - 기능 확장 모듈
 * 원본 객체의 뼈대를 유지한 채 "기능만 동적으로 추가"하는 덮어쓰기 방식입니다.
 * 
 * 핵심 이점
 * 1. 원본 보존: 기존 클래스나 객체의 코드를 직접 수정하지 않고도 새로운 기능을 확장합니다.
 * 2. 조합의 유연성: 여러 개의 데코레이터를 중첩하여 다양한 기능 조합을 자유롭게 만듭니다.
 * 3. 상속의 대안: 복잡한 상속 구조 없이도 객체에 책임(Voice Control 등)을 가볍게 부여합니다.
 */
export const withVoiceControl = (device) => {
  const decorator = {
    decorated: 'AI 음성 제어 모듈 활성화됨'
  }
  
  // TODO 6: 전달받은 기기(device)에 AI 음성 제어 모듈을 활성화해서 내보냄
  
}