{
  "name": "json-crud-test-server",
  "scripts": {
    "start": "json-server --watch db.json --port 4000",
    "prod": "vite build",
    "build": "vite build"
  },
  "dependencies": {
    "json-server": "0.17.4"
  }
}